ORIGIN = 'http://localhost:8080'
# Sentry error tracking (optional — leave blank to disable)
SENTRY_DSN=
# Redis persistence (optional — leave blank to run purely in-memory).
# Full connection string incl. credentials, e.g.
#   redis://default:<password>@feisty-orange-pet-46987.db.redis.io:10660
# Use rediss:// if your Redis enforces TLS.
REDIS_URL=
